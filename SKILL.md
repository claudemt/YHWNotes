# Physics Lecture Notes — 通用物理讲义编写 Skill

本 Skill 用于编写和长期维护数学严格、物理清楚、新手友好、符号统一并达到出版质量的物理讲义。默认沿用成熟教材的组织方式，而不是论文摘要、公式汇编或提纲式写法。整本讲义应让读者沿连续的认知链前进：从已有知识和尚未解决的问题出发，说明为什么需要新的物理对象；随后给出明确的数学定义和最一般的理论结构，在一般结论已经建立之后，再代入具体系统参数、识别控制参数、施加受控近似，最后讨论特例、实验条件与应用。任何核心理论都不应依靠“先看一个熟悉例子，再猜出一般规律”的方式建立。

## 1. 数学要求

核心推导必须沿“最一般精确理论 → 精确或形式精确解 → 系统参数化 → 控制参数 → 受控近似 → 特例”的方向单向推进。若一般问题没有闭式解析解，应先给出 Green 函数、谱表示、积分方程、时间序指数、Dyson 展开、路径积分或其他形式精确表达，再进入近似。禁止先从二能级、单模、低反冲、Markov、弱耦合等方便模型出发，再用“类似地”“推广可得”把结果反推为一般结论。

重要结论必须真正算出来。Derivation 的作用不是复述正文，也不能用“显然”“容易得到”、定性描述或单独一个比例关系代替关键证明。对于算符代数、指标缩并、Lie 代数、自旋求和、传播子、圈积分、投影、有效理论消元、渐近展开、自伴性和误差估计等真正困难的步骤，应给出足够的中间等式，使读者可以复算。反过来，高斯积分、Gamma 函数、标准 Fourier 恒等式、基本 delta 函数性质等成熟数学工具不必为了显得详细而机械展开。篇幅应按认知难度分配，而不是按公式长度分配。

所有近似都必须可控制。应说明它从哪个精确方程开始，小参数或尺度比较是什么，保留到哪一阶，舍弃项是什么量级以及什么时候失效。精确性由公式本身体现，不要反复写“这里还没有做近似”；只有真正开始截断、展开或特殊化时才说明近似。

只给重要公式编号。重要定义、主动力学方程、后文会引用的结论、近似条件和最终可观测量需要编号，Derivation 中间步骤通常使用无编号的 `align*`、`equation*` 或 `aligned`。

## 2. 物理与叙述要求

任何新符号第一次出现时，都必须说明数学类型和物理意义；必要时还应说明所在空间、单位、归一化和与已有量之间的关系。全书建立统一 symbol registry：一个物理对象原则上只对应一个主要记号，同一符号不能在不同章节承担互不相关的意义。Fourier 符号、Minkowski 度规、规范、连续态归一化、Green 函数边界条件、矢量和算符字体以及单位制都应在全书层面一次确定。默认优先采用 SI 制并显式保留必要常数；若某一本讲义需要其他单位制，也必须在全书中统一。

新概念不能凭空出现。正文应先说明上一理论已经解决什么、还缺什么以及为什么现有对象不足，再引入新数学对象，说明定义、物理意义和它解决的问题。新手友好不是降低数学水平，而是减少认知跳跃。困难内容应获得更多解释，而不是相反。

正文必须独立可读。临时删除所有 Derivation 后，读者仍应知道每个量的含义、核心公式描述的物理、适用范围以及下一节为什么出现。正文负责 motivation、定义、物理图像、核心公式、一般结论和近似条件；Derivation 只负责草稿纸层面的复杂数学。重要结论不能藏在推导框最后一行。

讲义语言应像教师连续讲解，而不是 AI 提纲。尽量不用大量“第一、第二、第三”式强制分点，也不要频繁用 `\paragraph` 或粗体短语制造伪标题。能用自然过渡句承上启下时就不要再增加结构层级。删除“这一版”“按照要求”“这里没有使用近似”“为了避免误解”等作者元叙述和修订历史。

## 3. 图形要求

图形服务于物理理解。能够由理论公式计算的结果，应优先给定量 Python 图；TikZ 用于几何、过程、拓扑、能级、相互作用和数学映射等结构示意。Python 与 TikZ 使用统一视觉语言：Latin Modern 或与正文高度一致的衬线字体，中等偏大的最终印刷字号，统一线宽、刻度与标记风格。图内文字默认使用英文，caption 使用正文语言。

Python 图的 legend 原则上位于图内四角之一，并根据数据分布选择真正空的位置；逐图检查它是否遮挡曲线、误差带、极值或 annotation。不要为了省事默认把 legend 放到图外。所有 Python 图通过统一 style helper 设置字体、字号、线宽、tick 和保存参数，脚本命名采用 `semantic_name_code.py`，生成文件采用相同 stem 的 `semantic_name.pdf/png`。

TikZ 图首先保证数学和物理关系正确。箭头必须表示真实的演化、映射、投影、相互作用、包含或因果关系。同类箭头、节点、Feynman 线型和 momentum label 通过 `preamble.tex` 的公共 style 控制。复杂标签位置应根据节点坐标、切向量和法向量计算，并检查 bounding box，而不是完全依赖随意的 `xshift/yshift`。所有图必须在最终 PDF 页面尺寸下做视觉 QA，不能通过整体缩小来掩盖重叠。

## 4. LaTeX 与排版要求

多书工程默认采用 `Publication` 式单仓库组织：根目录只维护一个 `main.tex`、一个公共 `preamble.tex`、一个统一构建器 `main.py` 和一个 Python 图形样式 `preamble.py`；每本书位于 `content/<BookName>/`，其唯一入口为 `content/<BookName>/<BookName>.tex`。正式章文件直接命名为 `ch01.tex`、`ch02.tex`……并放在书目根目录，避免把章节名称和临时内容写进文件名；只有一个章节内部确实需要继续拆分时，才把稳定模块放入 `sections/`。前置材料可放 `frontmatter/`，附录或索引可放 `backmatter/`。一本书自己的 `settings.tex` 只能保存专用数学记号、额外专业宏包、资源路径和真正专用的 TikZ style，不得复制 geometry、页眉页脚、标题、结构体、hyperref 或 Python 全局样式。公共样式的修改必须在所有书上做回归测试。

数学排版保持统一：微分用正体 `\dd`，虚数单位和自然指数分别采用正体 `\ii`、`\ee`，矢量、矩阵、算符和张量采用固定字体约定，单位由 `siunitx` 管理。复杂公式优先使用 `align`、`aligned`、`split` 或 `multlined` 并按真正的数学关系符号对齐；不使用整体缩放来掩盖公式布局问题。

所有交叉引用都采用语义 label。label 使用小写英文与连字符，例如 `eq:dirac-current`、`fig:dispersion-recoil`、`sec:gauge-fixing`，不得使用绝对编号作为 label。公式、图、表、节、章的引用通过统一宏完成，使“式/图/表/第…节 + 编号”整体为蓝色链接。全书只保留少量真正有教学作用的 `Derivation`、`Example`、`Note` 环境，标题简短，Derivation 默认位于局部结构末尾。

## 5. 内容结构要求

目录层级尽量限制在 `chapter → section → subsection` 三层。正文中不要频繁使用 `subsubsection`、`paragraph` 或粗体短标题。一个典型 subsection 应先自然承接前文并交代 motivation，再给定义、物理图像和核心方程，随后解释一般结论与适用条件；必要时加入 Example 或 Note，最后放 Derivation。并非每节都必须机械包含这些结构体。

Example 必须建立在一般理论之后，不能用具体模型代替母理论。Note 只承担边界条件、易错点、历史背景或数学旁注；删除全部 Example/Note 后主理论仍应闭合。删除全部 Derivation 后正文仍应完整可读。

## 6. 审计方法

长篇讲义采用“静态搜索 → 符号审计 → 逐式数学审计 → 结构审计 → 图形审计 → 最终 PDF QA”的多层流程。源码先搜索过深标题、粗体伪标题、作者元叙述、`显然/容易得到/类似可得/推广可得`、不规范微分、混用矢量命令、硬编码编号、无语义 label、裸 `\ref/\eqref`、未引用图和异常公式缩放。符号审计检查重要符号的第一次出现、单位和后续意义；逐式审计检查符号、量纲、指标、共轭、负号、积分测度、`2\pi`、`\hbar`、`c`、Jacobian、简并度和归一化。

Derivation 逐个检查是否真的增加了数学内容，还是只重复正文；新手可读性通过“临时删除全部 Derivation 后连续阅读”检查。Python 和 TikZ 必须直接在最终 PDF 上逐图看字号、legend、节点、箭头、label、裁切与物理方向。完整编译至少要求没有 LaTeX error、undefined reference、undefined control sequence 和明显 overfull box。

用户指出的任何局部错误都必须先抽象为一个错误类别，再全书搜索同类问题。发现一个微分格式错误，就检查所有积分测度；发现一个 TikZ label 重叠，就审查所有同类图；发现一个新符号未定义，就检查所有重要符号的首次出现。局部反馈必须升级为全局 invariant，而不是只做单点 patch。

最终验收同时通过数学、物理、教学、符号一致性、结构和出版质量六道门。可以把整套 Skill 压缩成四句话：**从已知走向未知，从一般走向特殊；正文负责让读者理解物理，推导负责把数学真正算完；符号、约定和视觉语言在全书范围保持一致；任何局部错误一旦出现，都必须升级为全局同类审计。**
