\begin{tikzfig}[x=1cm,y=1cm]
    % =========================
    % Parameters
    % =========================
    \pgfmathsetmacro{\xM}{0.0}
    \pgfmathsetmacro{\tLens}{0.26}
    \pgfmathsetmacro{\xFront}{\xM-\tLens}
    \pgfmathsetmacro{\xBack}{\xM+\tLens}
    
    \pgfmathsetmacro{\xCOne}{-3.0}
    \pgfmathsetmacro{\xCTwo}{3.0}
    \pgfmathsetmacro{\xZtwo}{2.4}
    
    \pgfmathsetmacro{\xLeft}{-5.0}
    \pgfmathsetmacro{\xRight}{5.0}
    \pgfmathsetmacro{\Hh}{2.2}
    
    \pgfmathsetmacro{\wL}{0.45}
    \pgfmathsetmacro{\zRL}{1.35}
    \pgfmathsetmacro{\wR}{0.45}
    \pgfmathsetmacro{\zRR}{1.35}
    
    \pgfmathsetmacro{\Lw}{1.8}
    \pgfmathsetmacro{\Lz}{1.2}
    
    \pgfmathsetmacro{\yTop}{1.55}
    \pgfmathsetmacro{\yBot}{-1.55}
    \pgfmathtruncatemacro{\Nsamp}{18}
    
    % =========================
    % Styles
    % =========================
    \tikzset{
        env/.style={thick},
        dir/.style={
            postaction={decorate},
            decoration={markings, mark=at position 0.9999 with {\arrow{Stealth[length=2.2mm]}}}
        }
    }
    
    % =========================
    % Axis + dashed planes
    % =========================
    \draw[thick] (\xLeft,0) -- (\xRight,0);
    
    \draw[densely dashed,thick] (\xCOne,-\Hh) -- (\xCOne,\Hh);
    \draw[densely dashed,thick] (\xZtwo,-\Hh) -- (\xZtwo,\Hh);
    \draw[densely dashed,thick] (\xCTwo,-\Hh) -- (\xCTwo,\Hh);
    
    % =========================
    % Lens
    % =========================
    \draw[thick]
    (\xFront,2.0) .. controls (\xM-0.65,1.05) and (\xM-0.65,-1.05) .. (\xFront,-2.0);
    \draw[thick]
    (\xBack,2.0) .. controls (\xM+0.65,1.05) and (\xM+0.65,-1.05) .. (\xBack,-2.0);
    
   % ---- Left snippet around xCOne ----
\pgfmathsetmacro{\xLmin}{\xCOne-\Lw}
\pgfmathsetmacro{\xLmax}{\xCOne+\Lw}

\draw[env,dir,domain=\xLmin:\xLmax,samples=80]
  plot (\x, {\wL*sqrt(1 + ((\x-\xCOne)/\zRL)^2)});
\draw[env,dir,domain=\xLmin:\xLmax,samples=80]
  plot (\x, {-\wL*sqrt(1 + ((\x-\xCOne)/\zRL)^2)});

% ---- Right snippet around xCTwo ----
\pgfmathsetmacro{\xRmin}{\xCTwo-\Lw}
\pgfmathsetmacro{\xRmax}{\xCTwo+\Lw}

\draw[env,dir,domain=\xRmin:\xRmax,samples=80]
  plot (\x, {\wR*sqrt(1 + ((\x-\xCTwo)/\zRR)^2)});
\draw[env,dir,domain=\xRmin:\xRmax,samples=80]
  plot (\x, {-\wR*sqrt(1 + ((\x-\xCTwo)/\zRR)^2)});


    % =========================
    % Distance arrows
    % =========================
    \draw[<->,thick] (\xCOne,\yTop) -- (\xFront-0.1,\yTop);
    \node[above] at ($(\xCOne,\yTop)!0.5!(\xFront,\yTop)$) {$z_{01}$};
    
    \draw[<->,thick] (\xBack+0.1,\yTop) -- (\xZtwo,\yTop);
    \node[above] at ($(\xBack,\yTop)!0.5!(\xZtwo,\yTop)$) {$z_2$};
    
    \draw[<->,thick] (\xBack+0.1,\yBot) -- (\xCTwo,\yBot);
    \node[below] at ($(\xBack,\yBot)!0.5!(\xCTwo,\yBot)$) {$z_{02}$};
\end{tikzfig}
