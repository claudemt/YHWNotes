\begin{tikzfig}[x=0.5cm,y=0.5cm]
    \begin{scope}
    \foreach \x in {0,...,3}{
      \foreach \y in {0,...,4}{
        \ifnum\x<3
          \draw[black!70] (\x,\y)--(\the\numexpr\x+1\relax,\y);
        \fi
        \ifnum\y<4
          \draw[black!70] (\x,\y)--(\x,\the\numexpr\y+1\relax);
        \fi
      }
    }

    \foreach \x in {0,...,3}{
      \foreach \y in {0,...,4}{
        \fill[black] (\x,\y) circle (1.1pt);
      }
    }

    \draw[green!70!black, line width=0.9pt] (1,2) circle (0.30);
    \draw[red!80!black,   line width=0.9pt,dashed] (3,4) circle (0.30);

    \node[below] at (1.5,-1.0) {original $4\times 5$};
  \end{scope}

  \begin{scope}[xshift=3.2cm]

    \foreach \x in {0,...,7}{
      \foreach \y in {0,...,9}{
        \ifnum\x<7
          \draw[black!70] (\x,\y)--(\the\numexpr\x+1\relax,\y);
        \fi
        \ifnum\y<9
          \draw[black!70] (\x,\y)--(\x,\the\numexpr\y+1\relax);
        \fi
      }
    }

    \foreach \x in {0,...,7}{
      \foreach \y in {0,...,9}{
        \fill[black] (\x,\y) circle (1.0pt);
      }
    }

    \draw[green!70!black, line width=0.9pt] (1,2) circle (0.30);
    \draw[green!70!black, line width=0.9pt] (6,2) circle (0.30);
    \draw[green!70!black, line width=0.9pt] (1,7) circle (0.30);
    \draw[green!70!black, line width=0.9pt] (6,7) circle (0.30);

    \draw[red!80!black, line width=0.9pt,dashed] (3,4) circle (0.30);
    \draw[red!80!black, line width=0.9pt,dashed] (4,4) circle (0.30);
    \draw[red!80!black, line width=0.9pt,dashed] (3,5) circle (0.30);
    \draw[red!80!black, line width=0.9pt,dashed] (4,5) circle (0.30);

    \node[below] at (3.5,-1.0) {symmetrized $8\times 10$};
  \end{scope}
\end{tikzfig}
